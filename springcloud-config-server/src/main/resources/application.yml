server:
  port: 7080
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Panlf/springcloud-learn-config.git
          search-paths:
          # 读取文件夹名(目录地址)
            - learn